using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShiftGrid : MonoBehaviour
{
    private SpriteRenderer art;
    void Start()
    {
        art = GetComponent<SpriteRenderer>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
